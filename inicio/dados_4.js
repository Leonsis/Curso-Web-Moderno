// A variavel a está recebendo o valor 3
let a = 3;

// ...

// A varialvel a está sendo reatribuida com o valor 10
a = 10;

// ...

console.log(a);

// Em js para colocarmos uma variavel como constante, usamos a palavra-chave const1

const b = 4;
// Assim que a variavel é declarada como constante, ela não pode ser reatribuida com um novo valor.

b = 8; // Isso vai gerar um erro, pois a variavel b é constante e não pode ser reatribuida.

console.log(b);